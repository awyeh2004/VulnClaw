# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
build/
dist/
eggs/
pip-wheel-metadata/
.mypy_cache/
.pyre/
.ruff_cache/
.tox/
.nox/

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
Desktop.ini

# VulnClaw runtime
.vulnclaw/
.workbuddy/
.trae/
sessions/
reports/
pocs/
vulnclaw-output/

# Test / coverage
.pytest_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml

# Environment / secrets
.env
.env.*
!.env.example

# Temp / local files
*.tmp
*.bak
*.log
_test_output*.txt
_test_report_raw.txt

# Agent/CTF 解题临时文件(测试请求/响应、注入 payload 实验)
# 做题过程的中间产物一律不入库,保留在本地工作目录
/fpc*.txt
/pl_*.txt
/pv*.txt
/pv*.html
/body*.txt
/c[0-9].txt
/c[0-9].html
/e[0-9].html
/vt*.txt
/vt*.html
/m_sleep.txt
/m_undef.txt
/o_echo.txt
/o_ls.txt
/sh[0-9].txt
/payload_post.txt
/wwwsrc/
/dis[0-9]*.txt
/dis_full*.txt
/solve_*.log
/solve_*.err.log
/swarm_*.log

# Frontend build / deps
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.npm-cache/
frontend/*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.cjs
frontend/vite.config.d.ts
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# AI tooling (local)
.agents/
.claude/
skills-lock.json
CLAUDE.md

# Local docs / local-only artifacts
docs/
.test-tmp/
版本号文件.png

# 开发中途产生的临时文件
.projectHelp/
# Rust TUI build artifacts
tui/target/

# IR 便携工具箱 (~359MB: Sysinternals/NirSoft/D盾 的 EXE 与 DLL、volatility3 pylib)
# 审计 round-3 发现 D: 未跟踪的二进制会被一次 `git add -A` 灌进仓库历史。
# 清单与哈希见 .ir-tools/MANIFEST.md / _manifest.tsv（随工具箱本地保存）。
.ir-tools/

# IR 演练靶机（本地教学脚手架，含故意的"后门"痕迹样本与"答案卡"，不应入库）
#
# 注意：整个 demo/ 都要忽略，不只是 drill 子目录。
#
# drill 的 setup 脚本 / README / ANSWER-KEY 本身就是答案：agent 若从工作目录
# 读到它们会直接抄答案而不做取证（实测发生过 —— shell_command 的 cwd 是仓库根，
# agent 的 `ls` 就会撞上这些文件）。
#
# 因此演练现已整体移出仓库，放在 D:\ir-drill\（见 demo/README.md）。
# 这里保留 demo/ 是为了防止将来有人又把演练脚手架放回仓库。
#
# 同一条教训在靶机内部也成立：setup 脚本拷进容器 /tmp 后忘记删除，
# `docker commit` 会把它们烘进镜像，agent 一条 `ls -al /tmp` 就能读到答案。
# setup.ps1 已用 finally 删除脚本 + commit 前后双重校验 /tmp。
demo/


# TSecBench 跑分本地产物 (审计 B1: 含 token/会话 cookie/内网 IP, 严禁入库)
tsec_bench/
tsec.env
cookies.txt
cook.txt
ih.db
quiz_drill/
quiz.html
.zcode/
work_puzzle/
work_*/

# DataCon/CTF 解题一次性产物 (审计 round-6 #7: 含 .exe 与硬编码靶机 IP)
brute*.c
brute*.exe
test_sha.*
brute_flag.py
poll*.py
scan_full.py
disasm*.txt
out*.txt

# DataCon 解题产物 (探针脚本/页面快照/cookie/块分析, EncFlask 会话数据)
adm_home.html
alive.html
body_*.bin
cj.txt
c0.txt
c1.txt
c2.txt
hello.asm
fg_*.html
forged_*.html
gsearch*.py
h2.html
kk*.txt
lognew.txt
marker.txt
md5test.py
out*.html
out1.html
p_b64.txt
p_ssrf*.py
page_*.html
probe*.py
pwn_tmp/
regnew.txt
upstream_tree/
vres.txt
w1block.txt
w*.html
w*.txt
